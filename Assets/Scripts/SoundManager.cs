using UnityEngine;
using UnityEngine.Audio;
using UnityEngine.UI;

public class SoundManager : MonoBehaviour
{
    // from Youtube video: https://youtu.be/G-JUp8AMEx0?si=dMH4HJjcogy8fmLB

    [SerializeField] private AudioMixer myMixer; // audio mixer
    [SerializeField] private Slider musicSlider; // slider

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    
}
